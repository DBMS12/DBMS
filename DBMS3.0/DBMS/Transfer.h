#pragma once
class Transfer
{
private:
	//static Field* m_oldField;
public:
	Transfer();
	~Transfer();
//	static void OldField(Field oldField); // 保留要修改的字段信息，继而传递给dlg
	//static Field* GetOldField(); // dlg获取旧的字段，并在初始化的时候显示出来
};

